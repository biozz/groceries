# https://taskfile.dev

version: '3'

tasks:
  build:
    cmds:
      - go build -o bin/groceries main.go
  run:
    cmds:
      - ./groceries
    deps: [build]
    sources:
      - static/*
      - main.go
      - index.html
